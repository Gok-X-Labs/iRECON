# ── Core server ───────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
slowapi==0.1.9
# FastAPI uses pydantic for request/response models (BaseModel)
pydantic>=2.0.0
# FastAPI requires python-multipart for UploadFile / Form support
python-multipart>=0.0.9

# ── DNS / WHOIS ────────────────────────────────────────────────────────────────
dnspython==2.6.1
python-whois==0.9.4

# ── Build ──────────────────────────────────────────────────────────────────────
pyinstaller==6.10.0

# ── Email artifact extraction ──────────────────────────────────────────────────
# HTML parsing for display/link mismatch detection
beautifulsoup4==4.12.3
# PDF hyperlink extraction from annotation objects (optional — falls back to regex scan)
# Extracts clickable links including button widgets from PDF attachments
pypdf>=4.0.0

# ── QR code detection (image attachments) ─────────────────────────────────────
# cv2 QRCodeDetector — no system zbar library required
# opencv-python 4.10.0.84+ required for NumPy 2.x compatibility (4.9.x was built against NumPy 1.x)
# Uses stable ABI (cp37-abi3) — compatible with Python 3.7–3.13
opencv-python==4.10.0.84
# Pillow 11+ required for Python 3.13 (10.x has no cp313 wheels)
Pillow==11.1.0
# numpy 2.x required for Python 3.13
numpy==2.2.5

# ── QR code detection (PDF attachments) ───────────────────────────────────────
# Renders PDF pages to bitmaps for QR scanning
# Requires poppler system package — see README for install instructions:
#   Windows: https://github.com/oschwartz10612/poppler-windows/releases
#            Extract and add the bin\ folder to your system PATH
#   Ubuntu:  sudo apt install poppler-utils
#   macOS:   brew install poppler
pdf2image==1.17.0

# ── PSL-aware domain parsing ───────────────────────────────────────────────────
# Used by hostname_utils.py for accurate TLD/subdomain extraction
tldextract==5.1.2